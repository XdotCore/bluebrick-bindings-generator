mod Lego::GUI
use Lego::Events

[test attr]
class MainMenuScreen {
    0 = a: arr<10, ptr<fn<thiscall, (
        a: u32,
        b: fn<(c :    hello)>,
        d: ptr<arr<10, ptr<ptr<ThisType>>>>,
    ), fn<(e: ptr<arr<10, him>>)>>>>

    piss = <
        <_, thjis> = to
        <jkhs> {
            hello
        }
    >
}
